Line 53: ['§5 L125']
  Text: - *Ripple-effect guard*: Store-side event processing must enforce `event_id` uniqueness before applying GRN stock changes. If the store replays a GRN event after crash recovery without dedup, stock would be double-inflated. Apply the same `ON CONFLICT (event_id) DO NOTHING` pattern Central uses (§5 L125).

Line 114: ['§15 L387']
  Text: - *Note (adjusted from original)*: The original description incorrectly stated Central Admin had "no notification." §15 L387 already defines the trigger; the gap is the delivery channel, not the trigger itself.

Line 120: ['§3 L10']
  Text: - *Concurrency & Deadlock-Free Row-Locking Invariant*: Under `READ COMMITTED`, concurrent checkouts across Counters 1, 2, and 3 must prevent overselling low-stock batches while avoiding cyclic deadlocks on multi-item baskets. Mandate deterministic row-level locking: all inventory batch decrements within a checkout transaction must lock rows in strict ascending primary-key order (`SELECT id, available_base_qty FROM store_batches WHERE id = ANY(:batch_ids) ORDER BY id ASC FOR UPDATE;`). Because two-phase checkout commit (§3 L10) releases database locks within <10ms (before printer spooling or network I/O), deterministic sorting mathematically guarantees zero deadlocks across counters with zero perceived checkout latency.

Line 140: ['§5 L150']
  Text: - *Supersedes v1.6.5 Baseline*: Formally supersedes the colloquial term "Pharmacist PIN" in [architecture_v1_6_5.md §5 L150](architecture_v1_6_5.md#event-types), [§10.3 L298](architecture_v1_6_5.md#103-schedule-x--ndps-dual-prescription-custody--bound-ledger), and Automated Test 5 (which asserted "pharmacist PIN"), elevating Schedule X / NDPS dispensing from a transient Quick-PIN to Tier B Full Argon2id Password verification to establish statutory non-repudiation under Rule 65(4). On worker terminals (Counter 2/3), the pharmacist's full password entry executes a single-use TLS verification handshake with Counter 1.

Line 152: ['§5 L150']
  Text: 31. **Prescription Image Hash Verification on Retrieval**: §5 L150 already carries a "prescription image hash" on the dispense event, but §10.3 doesn't specify what this hash is used for. Specify that the SHA-256 hash in the dispense event is verified against the decrypted image when displayed for regulatory audit. If hash mismatch: display a corruption warning banner, block the image from being used as evidence, and log an integrity alert.

Line 189: ['§17 L406']
  Text: 32. **Database Migration Versioning Tool**: §17 mentions rollback scripts but no migration framework. Specify Alembic (Python/SQLAlchemy) with a `migration_history` table. Store-side migrations must run locally on the edge node during staged rollouts (§17 L406).

Line 197: ['§2 L40']
  Text: - *Store-Scoped Key Provisioning & Standby Failover Guard*: The TLS certificate and private key (`C:\medpos\config\certs\store_lan.crt` and `.key`) are store-scoped, not machine-scoped. They must be replicated to Counter 2 during initial setup under Windows ACLs (`icacls NT SERVICE\MedPOS`). When Counter 2 promotes to Primary via `promote_to_primary.bat` (§2 L40), it binds to the **identical store certificate**, preventing worker terminals (Counter 3) from rejecting Counter 2 due to certificate fingerprint mismatch.

Line 229: ['§13 L343']
  Text: 41. **Multi-User Same-Counter Login Prevention**: Block login when a shift is active on that terminal. Display message directing the incoming cashier to have the current shift closed or Manager force-closed (§13 L343).

Line 234: ['§3 L63', '§5 L160']
  Text: 47. **Patient Lookup at Checkout**: The patient directory already exists (§3 L63, §5 L160). The gap is the *search UX at checkout*: phone number search → disambiguate if multiple matches → auto-populate patient details → link to sale event's `customer reference` field.

Line 242: ['§6 L219', '§6 L208']
  Text: 18. **Damaged-on-Shelf Write-Off Workflow**: §6 defines `write-off` stock-moves but no discovery-initiated workflow. Specify: photo + reason code + Manager PIN approval before write-off commit, mirroring the `adjustment-in` flow (§6 L219) and the transfer breakage photo requirement (§6 L208).

Line 283: ['§13 L343']
  Text: - *Assertion B (Manager Emergency Force-Close)*: From Counter 3, authenticated as `store_manager`, invoke `/api/shifts/{counter_2_shift_id}/force-close` providing the physical cash count (`declared_cash`) and a supervisory note. Assert: HTTP 200 OK, shift state transitions to `CLOSED_FORCE` per §13 L343, and the Z-Report variance calculation successfully executes.

Line 287: ['§2 L54']
  Text: 53. **Clock-Skew Sync Warning Test**: Set store clock 6 minutes ahead. Perform sync heartbeat. Assert: UI warning triggered per §2 L54.

